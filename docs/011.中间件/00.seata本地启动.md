---
title: Seata本地启动
date: 2020-05-11 13:54:40
permalink: /pages/0bzvi
article: false,
tags: 
  - null
---

## 1. 创建Seata数据库和表结构

  创建名为 `seata` 的数据库，执行项目中 `docs/seata/seata.sql` 脚本文件完成 Seata 数据库初始化。([seata.sql 在线地址](https://github.com/seata/seata/blob/develop/script/server/db/mysql.sql))


## 2. Nacos创建Seata命名空间 

访问Nacos控制台， 新建Seata的注册中心和配置中心的命名空间 ，这里命名空间ID为 `seata-namespace-id`

![](https://www.youlai.tech/files/blog/1644499302242-9e4468c6-ca77-4a76-b43d-e78b191d6064.png)


## 3. 导入配置至Nacos


进入项目中 `docs/seata/config.txt` Seata的配置文件 ([config.txt 在线地址](https://github.com/seata/seata/blob/develop/script/config-center/config.txt))


修改Seata事务分组和数据存储相关信息，需要`修改`的 config.txt 信息示例如下：

```properties
# 事务分组配置格式：service.vgroupMapping.[事务分组] = TC集群名称
service.vgroupMapping.mall_tx_group=default
# 事务持久化模式
store.mode=db
store.db.dbType=mysql
store.db.driverClassName=com.mysql.cj.jdbc.Driver
store.db.url=jdbc:mysql://localhost:3306/seata?useUnicode=true&rewriteBatchedStatements=true
store.db.user=seata
store.db.password=seata
```

- mall_tx_group 是商城事务分组，default是TC的集群名称。[事务分组详情点击查看](http://seata.io/zh-cn/docs/user/transaction-group.html)
  

完成上述配置修改之后，在 Nacos 控制台新建 dataId 为 `seataServer.properties` 的配置，并将整个 config.txt 内容复制进去

![](https://s2.loli.net/2022/05/03/9zr4tjogad5TOHV.png)


还有Seata 1.4.2 版本新增指定的 data-id 为 `seataServer.properties` 配置文件有个问题，实践证明它不会从配置文件读取事务分组配置项即`service.vgroupMapping`，在SpringBoot应用启动时会报如下错误：

```bash
can not get cluster name in registry config 'service.vgroupMapping.mall_tx_group', please make sure registry config correct
```
所以还需在Nacos配置额外添加一项事务分组配置，data-id是service.vgroupMapping.mall_tx_group，配置内容是default，具体如下：

![20220503145302](https://s2.loli.net/2022/05/03/pLglmIhHasYGRtV.png)


## 4. 配置seata-server的注册和配置中心


进入项目中的 `middleware/seata/conf/registry.conf` 文件([registry.conf 在线地址](https://github.com/seata/seata/blob/develop/script/client/conf/registry.conf))

修改 seata-server 的注册和配置中心的配置如下:

```properties
# 注册中心
registry {
  type = "nacos"

  nacos {
    application = "seata-server"
    serverAddr = "localhost:8848"
    group = "SEATA_GROUP"
    namespace = "seata-namespace-id"
    username = ""
    password = ""
  }
}
#配置中心
config {
  type = "nacos"

  nacos {
    serverAddr = "localhost:8848"
    namespace = "seata-namespace-id"
    group = "SEATA_GROUP"
    username = ""
    password = ""
    dataId = "seataServer.properties"
  }
}
```
- seata-namespace-id：上文Nacos新增的Seata的命名空间ID
- seataServer.properties：上文Nacos新增存放config.txt配置的dataId

## 5. 本地启动seata-server

IDEA 打开命令行终端 Terminal，输入 `cd middleware/seata/bin` 切换到 Seata 的 bin 目录，执行 seata-server.bat 启动 seata-server 服务。


![20220503154226](https://s2.loli.net/2022/05/03/PUWYmCHqnI5uwDz.png)

完成启动之后,在Nacos服务列表的seata命名空间可以看到seata-server服务

![20220503154859](https://s2.loli.net/2022/05/03/TbhLCYzmOE67Kac.png)

至此本地启动 seata-server 完成。 