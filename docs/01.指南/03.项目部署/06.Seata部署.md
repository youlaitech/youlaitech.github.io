---
title: Seata部署
date: 2020-05-11 13:54:40
permalink: /pages/4vjq5
article: false,
categories: 
  - 指南
  - 项目部署
tags: 
  - null
---

## 官方链接

| 名称     | 地址                           |
| -------- | ------------------------------ |
| 官网地址 |[http://seata.io/zh-cn/]( http://seata.io/zh-cn/ )        |
| 源码仓库 | [https://github.com/seata/seata](https://github.com/seata/seata) |

## 版本声明

| 中间件名称 | 版本   | IP             |
| ---------- | ------ | -------------- |
| Seata      | 1.4.2  | 192.168.10.100 |
| Nacos      | 2.0.3  | 192.168.10.99  |
| MySQL      | 8.0.27 | 192.168.10.98  |

## 数据库创建

**1. 创建用户和授权(可忽略)**

```sql
# 降低MySQL密码策略等级
set global validate_password.length=4;
set global validate_password.special_char_count=0;
set global validate_password.policy =LOW;
# 创建用户
create user 'seata'@'%' IDENTIFIED BY 'seata';
# 授权
grant all privileges on seata.* to seata@'%';
flush privileges;
```

**2. 创建Seata数据库**

```sql
CREATE DATABASE seata DEFAULT CHARACTER SET utf8mb4 DEFAULT COLLATE utf8mb4_general_ci;
```

**3. 生成Seata表结构**

Seata表结构在线地址： [https://github.com/seata/seata/blob/develop/script/server/db/mysql.sql](https://github.com/seata/seata/blob/develop/script/server/db/mysql.sql)


## Seata安装前配置

**1. Nacos创建Seata命名空间**

访问Nacos控制台， 新建Seata的注册中心和配置中心的命名空间 ，这里命名空间ID为 `seata-namespace-id`

![youlai-mall Nacos](https://s2.loli.net/2022/05/04/O1bA4VumjJRpGHX.png)


**2. 导入Seata配置至Nacos**

在线获取Seata的配置文件：[https://github.com/seata/seata/blob/develop/script/config-center/config.txt](https://github.com/seata/seata/blob/develop/script/config-center/config.txt)

修改Seata事务分组和数据存储相关信息，需要`修改`的 config.txt 信息如下：

```properties
# 事务分组配置格式：service.vgroupMapping.[事务分组] = TC集群名称
service.vgroupMapping.mall_tx_group=default
# 事务持久化模式
store.mode=db
store.db.dbType=mysql
store.db.driverClassName=com.mysql.cj.jdbc.Driver
store.db.url=jdbc:mysql://192.168.10.98:3306/seata?useUnicode=true&rewriteBatchedStatements=true
store.db.user=seata
store.db.password=seata
```

`mall_tx_group` 是商城事务分组，default是TC的集群名称。[事务分组详情点击查看](http://seata.io/zh-cn/docs/user/transaction-group.html)
  

完成上述配置修改之后，在 Nacos 控制台新建 dataId 为 `seataServer.properties` 的配置，并将整个 config.txt 内容复制进去
![](https://s2.loli.net/2022/05/03/9zr4tjogad5TOHV.png)
Seata 1.4.2 版本读取上面的配置文件有个问题，实践证明它不会从配置文件读取事务分组配置项即`service.vgroupMapping`，在SpringBoot应用启动时会报如下错误：

```bash
can not get cluster name in registry config 'service.vgroupMapping.mall_tx_group', please make sure  registry config correct
```
所以在Nacos配置还需添加一条事务分组配置，data-id为service.vgroupMapping.mall_tx_group，配置内容是default，具 体如下：

![youlai-mall nacos](https://s2.loli.net/2022/05/03/pLglmIhHasYGRtV.png)


**3. 配置seata-server的注册和配置中心**

`registry.conf`是Seata的注册和配置中心的配置文件。[registry.conf文件在线预览](https://github.com/seata/seata/blob/develop/script/client/conf/registry.conf)

创建registry.conf文件
```bash
mkdir -p /mnt/seata/config
vim /mnt/seata/config/registry.conf
```

把下面完整的信息复制到 /mnt/seata/config/registry.conf 文件中

```properties
# registry.conf
registry {
  # file,nacos,eureka,redis,zk,consul,etcd3,sofa
  type = "nacos"

  nacos {
    application = "seata-server"
    serverAddr = "192.168.10.99:8848"
    group = "SEATA_GROUP"
    namespace = "seata-namespace-id"
    username = ""
    password = ""
  }
}

config {
  # file、nacos 、apollo、zk、consul、etcd3
  type = "nacos"

  nacos {
    serverAddr = "192.168.10.99:8848"
    namespace = "seata-namespace-id"
    group = "SEATA_GROUP"
    username = ""
    password = ""
    dataId = "seataServer.properties"
  }
}
```
- **type:**  注册中心和配置中心的类型，这里都选择 Nacos
- **seata-namespace-id** ： Nacos 为 Seata 配置的命名空间
- **username**：Nacos 用户名，Nacos 权限认证默认不开启，不用填
- **password**：Nacos 密码，Nacos 权限认证默认不开启，不用填
- **dataId**： Seata 1.4.2 新增，指定读取Nacos里 dataId 为 seataServer.properties 的配置

## Seata部署

官方部署步骤参考：[http://seata.io/zh-cn/docs/ops/deploy-by-docker.html](http://seata.io/zh-cn/docs/ops/deploy-by-docker.html)

**1. 准备工作**

```bash
# 创建日志挂载目录
mkdir -p /var/log/seata
```

**2. 下载镜像**

```bash
docker pull seataio/seata-server:1.4.2
```

**3. 创建和启动容器**

```bash
docker run -d --name seata --restart=always  \
-p 8091:8091 \
-e SEATA_IP=192.168.10.100 \
-e SEATA_CONFIG_NAME=file:/root/seata-config/registry \
-v /mnt/seata/config:/root/seata-config \
-v /var/log/seata:/root/logs/seata \
seataio/seata-server:1.4.2 
```
- SEATA_IP 默认是 Docker 容器的IP，一般需要设置成宿主机内网IP或公网IP以保证和其它应用的网络互通

**4. 启动验证**

在 Nacos 控制台的服务列表看到 Seata 服务。
![youlai-mall seata](https://s2.loli.net/2022/05/04/TxAIfpyerJ5jOP7.png)